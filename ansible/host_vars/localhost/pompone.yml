---
# Pompone-specific configuration

# Whether this project's Ansible playbooks should manage Docker on this host
manage_docker: true

# Docker Role variables
# Only used if `manage_docker: true` above.
# See https://github.com/geerlingguy/ansible-role-docker/blob/master/defaults/main.yml
# for available overrides.
docker_users:
  - "{{ ansible_user_id }}"

# Bot Identity
bot_nick: "Pompone"
bot_username: "pompone"
bot_realname: "Motociclismo Paesano"
bot_admin: "TedSki <email: tedski@efnetmoto.com>"
bot_owner: "TedSki"

# DCC/Telnet Port
dcc_port: &dcc_port 2020

# Docker compose environment variables
environment_variables:
  dcc_port: *dcc_port
  dcc_portrange: "{{ dcc_portrange_start }}-{{ dcc_portrange_end }}"
  stats_port: 8080  # Stats HTTP Port (nginx)
  cron_schedule: "{{ pisg_cron_schedule }}"
  tz: "America/Los_Angeles"
  shortener_api_key: "{{ vault_shortener_api_key }}"
  # Docker-internal URL of the url-shortener service on pompone-net
  shortener_api_url: "http://url-shortener:8080"
  brave_api_key: "{{ vault_brave_api_key }}"
  # Anti-troll join-gate (single-bot: Pompone only). See shared/tcl-scripts/joingate.tcl.
  joingate_channel: "#motorcycles"
  joingate_rbl_providers: "efnetrbl.org"
  joingate_soak_clean_seconds: 30
  joingate_soak_rbl_hit_seconds: 300
  joingate_op_response_window_seconds: 300
  joingate_rbl_lookup_timeout_seconds: 3
  joingate_enabled: 1

# Runtime directories to create
runtime_dirs:
  - "bots/Pompone/data"
  - "bots/Pompone/logs"
  - "bots/Pompone/logs/bot"
  - "bots/Pompone/logs/channels"
  - "bots/Pompone/pisg-cache"
  - "bots/Pompone/pisg-config"
  - "bots/Pompone/html"
  - "bots/Pompone/url-shortener-data"

# Separate log directories for pisg processing
separate_log_dirs: true

# Scripts to load (relative to container paths)
# (Scripts loaded from `scripts/` are Eggdrop-native and ship with the container)
bot_scripts:
  - "scripts/alltools.tcl"
  - "scripts/action.fix.tcl"
  - "scripts/dccwhois.tcl"
  - "scripts/userinfo.tcl"
  - "scripts/quotepong.tcl"
  - "scripts-shared/joingate.tcl"
  - "scripts-python-shared/searchbot.py"

# PISG Configuration
pisg_enabled: true
pisg_cron_schedule: "@hourly"  # Hourly
pisg_channels:
  - name: "#motorcycles"
    log_prefix: "motorcycles"
    format: "eggdrop"
    network: "Efnet"
    maintainer: "Pompone"
    output_file: "index.html"
