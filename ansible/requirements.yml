---
collections:
  - name: ansible.posix
    version: ">=2.1.0"
  - name: community.crypto
    version: ">=3.1.0"
  - name: community.docker
    version: ">=5.0.0"
  - name: community.general
    version: ">=12.4.0"

roles:
  - name: geerlingguy.docker  # used to manage docker installation
